using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RBTEST : MonoBehaviour
{
    Rigidbody2D rrr;
    // Start is called before the first frame update
    void Start()
    {
        this.rrr = GetComponent<Rigidbody2D>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
